services:
  - type: web
    name: ai-devops-assistant
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run build && npm start
    envVars:
      - key: NODE_VERSION
        value: 20
